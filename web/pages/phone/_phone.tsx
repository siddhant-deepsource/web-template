import { Phone } from '../../protobuf/phone/phone_pb';

interface PhoneComponentProp {
  phone: Phone.AsObject;
}

const PhoneComponent = (props: PhoneComponentProp): JSX.Element => <div>
    lol
  </div>;
